name: qlgd_lhk
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- Locked, Stable Versions to Resolve Conflicts ---
  cupertino_icons: 1.0.6
  flutter_riverpod: 2.4.9
  go_router: 13.2.0
  dio: 5.4.1
  flutter_secure_storage: 9.0.0
  intl: 0.20.2
  shared_preferences: 2.2.2
  freezed_annotation: 2.4.1
  json_annotation: 4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code Generation
  build_runner: 2.4.9
  freezed: 2.5.2
  json_serializable: 6.8.0

  # Linting
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
  generate: true # Needed for intl localization

  assets:
    - assets/images/
    - assets/l10n/
